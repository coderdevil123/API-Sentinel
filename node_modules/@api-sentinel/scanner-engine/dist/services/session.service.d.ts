export declare class SessionService {
    createSession(target: string): {
        scanId: `${string}-${string}-${string}-${string}-${string}`;
        target: string;
        startedAt: string;
    };
}
